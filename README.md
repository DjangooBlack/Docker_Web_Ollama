How to Run It

1.Save the above YAML as docker-compose.yml.

2.Run the following command in your terminal:

    Access the Web UI at http://localhost:3000.

    Signup with the email and password

    You should see this screen .
